# Runtime dependencies for the deployed app.
genblaze-core==0.3.7
genblaze-s3==0.3.6
# The demo generates on the page, so the GMI adapter ships now. Still no other
# provider adapters: nothing else is called at runtime.
genblaze-gmicloud==0.3.4
# Quality checks decode images server-side.
pillow==11.3.0
# Required to read and strip ID3 tags when verifying audio. genblaze treats
# this as optional, but audio verification is not optional for us.
mutagen==1.48.1
# Imported directly by hallmark.storage and hallmark.demo. All three only
# arrived here as transitive dependencies of the genblaze adapters, which is
# someone else's constraint to relax, so they are pinned where they are used.
# tests/test_packaging.py fails if a directly imported package goes undeclared.
httpx==0.28.1
boto3==1.43.57
botocore==1.43.57
# Reads Content Credentials, so the checker can answer for a file it did not
# make. Without it the only verdict a stranger's file can reach is "unsigned",
# including files carrying a valid signed credential from whoever generated
# them. Ships a native library, so it is the heaviest thing here by far.
c2pa-python==0.37.2
fastapi==0.140.7
python-multipart==0.0.20
python-dotenv==1.0.1
